import {chromium} from 'playwright';const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});const p=await b.newPage({viewport:{width:1440,height:1000}});p.on('pageerror',e=>console.log('ERROR',e.message));await p.goto('http://127.0.0.1:4179/work/hardline/');const r=p.locator('.hl-routing');await r.scrollIntoViewIfNeeded();
for(const stage of [0,1,2]){if(stage===0){await p.getByLabel('Origin',{exact:true}).fill('   ');await p.getByLabel('Destination',{exact:true}).fill('Facility');await r.getByRole('button',{name:/Continue/}).click();await r.getByRole('button',{name:'Use an illustrative example'}).click();}
console.log('BEFORE',stage,await r.innerText(),await r.locator('input,textarea').evaluateAll(es=>es.map(e=>({id:e.id,value:e.value,valid:e.checkValidity(),message:e.validationMessage}))));if(stage<2)await r.getByRole('button',{name:/Continue/}).click();}
await r.screenshot({path:'../hardline-probe.png'});console.log('OVERFLOW',await p.evaluate(()=>[...document.querySelectorAll('.hardline-page *')].filter(e=>e.getBoundingClientRect().right>innerWidth+1).map(e=>({tag:e.tagName,cls:e.className,parent:e.parentElement.className,rect:e.getBoundingClientRect().toJSON(),width:getComputedStyle(e).width,src:e.currentSrc})).slice(0,10)));await b.close();
