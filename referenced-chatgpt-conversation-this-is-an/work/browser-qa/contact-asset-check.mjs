import {chromium} from 'playwright';const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});for(let i=0;i<4;i++){const p=await b.newPage();p.on('requestfailed',r=>console.log('FAILED',r.url(),r.failure()));await p.goto('http://127.0.0.1:4179/contact/');await p.waitForTimeout(700);console.log(i,await p.locator('body').evaluate(e=>getComputedStyle(e).fontFamily));await p.close();}await b.close();
