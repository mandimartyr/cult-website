import {chromium} from 'playwright';import fs from 'node:fs';import assert from 'node:assert/strict';const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});const p=await b.newPage({viewport:{width:1440,height:1000},reducedMotion:'reduce'});await p.goto('http://127.0.0.1:4179/contact/');await p.evaluate(()=>document.fonts.ready);await p.addScriptTag({content:fs.readFileSync('node_modules/.pnpm/axe-core@4.13.0/node_modules/axe-core/axe.min.js','utf8')});const result=await p.evaluate(async()=>({route:'contact',width:1440,overflow:document.documentElement.scrollWidth-innerWidth,headings:[],violations:(await axe.run(document.querySelector('main'))).violations.map(v=>v.id),errors:[]}));assert.equal(result.overflow,0);assert.deepEqual(result.violations,[]);const results=JSON.parse(fs.readFileSync('../riso-site.json'));results[results.findIndex(r=>r.route==='contact'&&r.width===1440)]=result;fs.writeFileSync('../riso-site.json',JSON.stringify(results,null,2));await p.goto('http://127.0.0.1:4179/work/');await p.setViewportSize({width:810,height:725});await p.locator('.print-close').scrollIntoViewIfNeeded();await p.evaluate(()=>document.fonts.ready);assert(await p.locator('.print-close').evaluate(e=>e.getBoundingClientRect().height)<700);await p.locator('.print-close').screenshot({path:'../work-close-final-810.png'});console.log('Contact recheck and compact Work CTA passed');await b.close();
